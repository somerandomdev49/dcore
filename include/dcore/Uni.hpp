#pragma once

// Macros for pointer clarification.

#define OWN /* An objct owns the pointer. (it allocates/deallocates it) */
#define REF /* A reference to an object through a pointer (e.g. a polymorphic class) */
